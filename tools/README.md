# `tools/` — one-off scripts (seed loader, eval harness, etc.)
# Each tool has its own subfolder. Run with `uv run python -m tools.<name>.<entry>`
# from inside `server/` (so that the `nutriroll` package is importable).
