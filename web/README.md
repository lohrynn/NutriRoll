# NutriRoll web

Next.js 15 App Router + React 19 + TypeScript strict + Tailwind v4 +
shadcn/ui + Biome + next-intl + Serwist.

Run from the repo root: `make dev`.

## OpenAPI client

`lib/api/schema.d.ts` is generated. After any backend API change, run
`make gen-client` from the repo root.
